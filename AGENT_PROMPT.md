# پرامپت کامل ساخت اپلیکیشن «دارا» — دستیار جامع سرمایه‌گذاری

## نقش تو

تو یک ایجنت کدنویس اندروید حرفه‌ای هستی که قرار است روی یک پروژه‌ی موجود و نیمه‌تمام کار کنی و آن را به یک اپلیکیشن کامل و تولیدپذیر تبدیل کند. این سند، مشخصات کامل محصول است. آن را دقیق بخوان و مرحله‌به‌مرحله (فاز به فاز) پیش برو. بین فازها کد را کامپایل کن، تست بزن و کامیت جداگانه ثبت کن.

## محصول چیست؟

اپلیکیشن اندروید فارسیِ «دارا»: دستیار جامع مدیریت ثروت و سرمایه‌گذاری برای کاربر ایرانی. همه‌ی دارایی‌های کاربر (طلا، سکه، ارز، رمزارز، سهام، صندوق، خودرو، بانک) را یک‌جا و لحظه‌ای رصد می‌کند، ابزارهای تحلیلی و محاسباتی تخصصی بازار ایران می‌دهد، با هوش مصنوعی تحلیل روزانه ارائه می‌کند و برنامه‌ریزی مالی شخصی (سررسید، بده‌بستان، هدف‌گذاری) را انجام می‌دهد.

شعار: «همه‌ی دارایی‌ات، یک‌جا، به زبان خودت.»

## وضعیت موجود پروژه (این‌ها از قبل هست — خرابشان نکن)

پروژه در همین ریپازیتوری موجود است. پشته‌ی فنی:

- زبان: کاتلین، رابط کاربری: جت‌پک کامپوز + متریال ۳
- معماری: مدل-ویو-مدل به‌همراه ریپازیتوری؛ لایه‌ها: `data/local` (روم + دیتابیس رمزنگاری‌شده با اس‌کیوال‌سایفر)، `data/remote` (رتروفیت + موشی)، `data/repository`، `ui/screens`، `ui/viewmodel`، `navigation`، `security`، `worker`، `util`
- دیتابیس: روم با مایگریشن‌های واقعی (استفاده از `fallbackToDestructiveMigration` مطلقاً ممنوع)
- شبکه: رتروفیت + اوکی‌اچ‌تی‌تی‌پی + اینترسپتور لاگ
- پس‌زمینه: ورک‌منیجر (هشدار قیمت `PriceAlertWorker` و یادآور `ReminderWorker` موجود است)
- امنیت: پین نمک‌دار با محدودیت تلاش، قفل زیست‌سنجی (`androidx.biometric`)، رمزنگاری با `security-crypto`
- ذخیره‌سازی تنظیمات: دیتااستور
- هوش مصنوعی: کتابخانه‌ی فایربیس ای‌آی (جمنای) وصل است
- تست: روبولکتریک + روبورازی + تست‌های کامپوز

قابلیت‌هایی که از قبل ساخته شده: پورتفوی چنددارایی با خرید/فروش و سود محقق‌شده، حساب‌های بانکی و انتقال، واریز/برداشت، قیمت‌های زنده‌ی برس‌اِی‌پی‌آی، صفحه‌ی بورس تی‌اس‌ای‌ام‌سی، کریپتو با کوین‌مارکت‌کپ و موتور امتیازدهی (`crypto/ScoringEngine.kt`)، مبدل ارز، ماشین‌حساب سپرده و وام (ساده و مرکب)، تورم، هشدار قیمت، یادآور، آنبوردینگ و امنیت.

## منابع داده و کلیدها

- بازار ایران (دلار، طلا، سکه، ارزها): برس‌اِی‌پی‌آی (`brsapi.ir`) — کلید در `.env` با نام `BRSAPI_KEY` (به `BuildConfig` تزریق می‌شود؛ آگاه باش که داخل اِی‌پی‌کی قابل استخراج است)
- بورس: رابط‌های `TsetmcApiService` موجود
- رمزارز: کوین‌مارکت‌کپ — کلید `CMC_API_KEY` در `.env`
- هوش مصنوعی: جمنای از طریق فایربیس (`firebase-ai`)
- برای هر منبع جدید (مثلاً صندوق‌ها از فیپیران، اخبار از آر‌اس‌اس)، اول سرویس رتروفیت بساز، بعد ریپازیتوری، بعد مدل‌های روم برای کش آفلاین.

## قواعد سخت (هرگز نقض نشود)

1. هر تغییر اسکیمای دیتابیس باید مایگریشن صریح روم داشته باشد؛ حذف و بازسازی ممنوع.
2. ایمپورت پشتیبان باید اعتبارسنجی و در یک تراکنش اتمیک انجام شود (الگوی موجود را حفظ کن).
3. پین با نمک تصادفی و قفل‌کردن پس از تلاش‌های ناموفق — الگوی موجود را حفظ کن.
4. فروش دارایی باید موجودی را اعتبارسنجی کند و حذف خریدِ استفاده‌شده در فروش مسدود باشد.
5. داده‌ی نمونه فقط در بیلد دیباگ ساخته شود.
6. کل رابط کاربری راست‌به‌چپ و فارسی است: تاریخ شمسی (کتابخانه‌ی موجود یا `ThreeTen` + تبدیل)، اعداد فارسی در نمایش، فونت فارسی «وزیرمتن» به‌صورت فونت پیش‌فرض اپ.
7. هیچ کلید جدیدی هاردکد نشود؛ همه از `.env` با پلاگین Secrets بیایند.
8. هر فیچر: اول مدل داده و دائو، بعد ریپازیتوری، بعد ویومدل، بعد صفحه؛ با تست واحد برای منطق محاسباتی.
9. هیچ سیگنال خرید/فروش قطعی یا وعده‌ی سود نباید تولید شود؛ خروجی‌های تحلیلی همیشه با سلب مسئولیت نمایش داده شوند.
10. کار نیمه‌تمام کریپتو (تغییرات کامیت‌نشده‌ی `CryptoScreen`، `CryptoViewModel` و پوشه‌ی `crypto/`) را اول مرتب و کامل کن و کامیت کن، بعد وارد فازهای جدید شو.

## نقشه‌ی کامل فیچرها (محصول نهایی)

### ستون ۱ — پورتفوی یکپارچه چندسبدی
- ثبت همه نوع دارایی: طلا و سکه (با وزن و عیار)، ارز، رمزارز، سهام، صندوق، خودرو، نقره، بانک
- ارزش لحظه‌ای کل به تومان و دلار؛ سود/زیان کل و روزانه به مبلغ و درصد
- سود محقق‌شده هنگام فروش (موجود — حفظ شود)
- چند سبد مجزا با نام دلخواه (شخصی، هدف، آزمایشی و…)
- نمودار دایره‌ای سهم هر دارایی + نمودار روند ارزش سبد در زمان
- برچسب‌گذاری استراتژی روی هر خرید: «نوسان‌گیری»، «بلندمدت»، …
- خروجی گرفتن از سبد به صورت فایل اکسل و پی‌دی‌اف

### ستون ۲ — دیده‌بان قیمت و بازار
- تابلوی لحظه‌ای: دلار، یورو، درهم و ارزهای مسافرتی، طلا ۱۸ و ۲۴ عیار، انس جهانی، سکه‌های امامی/بهار/ربع/گرمی/پارسیان، نقره، رمزارزها، خودرو (بازار و کارخانه)
- **حباب سکه و طلا**: محاسبه و نمایش صریح حباب (قیمت روز منهای ارزش ذاتی بر مبنای انس جهانی، دلار و دلار نیما/سنا)
- بورس: شاخص کل و هم‌وزن، ارزش معاملات، نقشه‌ی حرارتی بازار، نمادهای پرتراکنش، صنایع برتر، حجم‌های مشکوک، پول هوشمند، عقربه‌ی نسبت خرید حقیقی، سهام عدالت
- اطلاعیه‌های کدال برای نمادهای سبد کاربر + عرضه‌های اولیه جدید با اعلان
- صندوق‌های سرمایه‌گذاری (سهامی/مختلط/درآمد ثابت): بازدهی، نمودار، مقایسه

### ستون ۳ — تحلیل و هوش مصنوعی
- تست شخصیت و ریسک‌پذیری (۱۰ تا ۱۲ سؤال، نتیجه در ۴ سطح) + چینش پیشنهادی سبد برای هر سطح
- امتیاز اختصاصی هر دارایی از موتور امتیازدهی موجود (`ScoringEngine`) — توسعه‌ی آن به بازار طلا و سکه
- تحلیل روزانه‌ی خلاصه و قابل‌فهم بازار با جمنای (چند جمله فارسی روان + یک نکته‌ی عملی برای سبد کاربر)
- اسکن هوشمند فاکتور و رسید با دوربین + جمنای: استخراج مبلغ، وزن، اجرت، تاریخ از فاکتور طلا/صرافی برای ثبت سریع خرید (همیشه با تأیید کاربر ذخیره شود)

### ستون ۴ — ابزارهای مالی
- مبدل حرفه‌ی ارز و رمزارز (موجود — حفظ شود)
- ماشین‌حساب سپرده و وام (موجود — حفظ شود)
- محاسبه‌گر اجرت و مالیات طلا بر اساس فرمول اتحادیه (قیمت تمام‌شده طلای نو و دست‌دوم)
- محاسبه‌گر حباب خرید (چند وقت دیگر با همین پول چند گرم طلا می‌خرد — قدرت خرید طلا)
- ماشین‌حساب گذشته‌نگر: «اگر فلان تاریخ فلان مقدار می‌خریدی امروز چقدر می‌شد؟»
- شبیه‌ساز سناریو: «اگر دلار بشود ایکس، ارزش سبد من چقدر می‌شود؟» (کلیدواژه‌ی قیمت‌ها + اعمال ضریب روی یک دارایی + محاسبه‌ی مجدد کل سبد)
- محاسبه‌گر افت قیمت خودرو (کارکرد، سال ساخت، قطعات رنگ‌شده)
- محاسبه‌ی افزایش سرمایه و سود آربیتراژ

### ستون ۵ — برنامه‌ریزی مالی
- سررسیدها: قسط، قبض، چک، اجاره — با تاریخ شمسی و اعلان (یادآور موجود را کامل کن با صفحه‌ی مدیریت سررسید)
- دفتر بده و بستان به تفکیک اشخاص + خالص مطالبات
- هدف‌گذاری خرید (گوشی، سکه، خودرو و…) با درصد پیشرفت بر اساس پس‌انداز و قیمت روز هدف

### ستون ۶ — امنیت و ابری
- پین و زیست‌سنجی (موجود)
- **حالت حریم خصوصی**: تاگل مخفی‌کردن همه‌ی مبالغ با نقطه‌چین، از تنظیمات امنیت
- پشتیبان‌گیری محلی رمزنگاری‌شده با رمز کاربر (موجود) + پشتیبان‌گیری خودکار روزانه ابری رمزنگاری‌شده (درایو شخصی کاربر)
- مخفی‌سازی خودکار مبالغ هنگام گرفتن اسکرین‌شات و در لیست اخیرها

### ستون ۷ — تجربه کاربری
- ویجت صفحه‌ی اصلی گوشی: ارزش کل سبد + قیمت زنده دلار و طلای ۱۸ (به‌روزرسانی هوشمند بدون خوردن باتری)
- اخبار تفکیک‌شده‌ی بازارها + ذخیره‌ی مورد علاقه‌ها
- جستجوی سراسری (نماد، ارز، صفحه، ابزار)
- راهنمای کوتاه فارسی برای هر ابزار محاسباتی

### خارج از محدوده (فعلاً نساز)
- خرید و فروش آنلاین، اتصال به کارگزاری، احراز هویت — نیازمند مجوز است
- شبکه‌ی اجتماعی کامل (هاب) — در آینده‌ی دور؛ فعلاً فقط واچ‌لیست محلی
- هر نوع سیگنال قطعی خرید/فروش

## فازبندی اجرا (به همین ترتیب پیش برو)

**فاز صفر — نظافت و زیرساخت فارسی**
1. کار کریپتوی کامیت‌نشده را کامل و مرتب کن، کامیت بزن
2. فونت وزیرمتن را پیش‌فرض کل اپ کن (فایل فونت در `assets`، تم کامپوز)
3. اعداد فارسی و تاریخ شمسی را در همه‌ی نمایش‌ها یکدست کن (یک یوتیلیتی واحد)
4. حالت حریم خصوصی (تاگل + ماسک مبالغ)

معیار پذیرش: اپ بیلد شود، همه‌ی صفحه‌ها فونت واحد و اعداد فارسی داشته باشند، تاگل حریم خصوصی کار کند.

**فاز یک — ابزارهای طلا و محاسبات**
1. محاسبه‌گر حباب سکه و طلا (به تابلوی قیمت هم وصل شود)
2. محاسبه‌گر اجرت و مالیات اتحادیه
3. ماشین‌حساب گذشته‌نگر
4. شبیه‌ساز سناریو
5. هر چهار مورد با تست واحد برای فرمول‌ها + ورود از «مرکز ابزارها»

معیار پذیرش: نتایج با چند نمونه‌ی دستی واقعی مطابقت دهد.

**فاز دو — تکمیل بازار**
1. صندوق‌ها از فیپیران (کش روم + مقایسه)
2. کدال و عرضه‌های اولیه با اعلان ورک‌منیجر
3. حجم مشکوک، پول هوشمند، صنایع برتر، سهام عدالت در صفحه‌ی بورس
4. خودرو: جدول کامل‌تر + محاسبه‌گر افت

**فاز سه — هوش مصنوعی**
1. تست ریسک‌پذیری + سبد پیشنهادی
2. تحلیل روزانه‌ی جمنای (کش روزانه، یک بار در روز صبح)
3. اسکن فاکتور با دوربین (دپندنسیز کامنت‌شده‌ی دوربین را فعال کن)

**فاز چهار — ویجت و اخبار**
1. ویجت سبد و قیمت‌ها
2. اخبار تفکیک‌شده + ذخیره‌سازی

**فاز پنج — ابری**
1. پشتیبان خودکار رمزنگاری‌شده ابری

## تعریف انجام‌شدن هر فیچر

- کاتلین کامپایل بدون خطا، بیلد دیباگ موفق
- تست واحد برای منطق محاسباتی/تبدیل داده پاس شود
- در حالت آفلاین با کش روم بالا بیاید و کرش نکند
- کامیت معنادار با پیام فارسی یا انگلیسی واضح
- هیچ هشدار امنیتی جدید (کلید هاردکد، متن ساده‌ی حساس) اضافه نشده باشد

شروع کن از فاز صفر.
